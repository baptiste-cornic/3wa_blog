<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../view/style.css">
    <title>3wa Blog Articles</title>
</head>
<body>
    <header>
        {% include 'templates/headerView.php'  %}
    </header>
    <main>
        <h2>3wa blog - articles</h2>
        <div class="content">
            <div>
                <h3>{{ article.title}}</h3>
                <p>{{ article.content }}</p>
                <p><em>Crée le : {{article.createdAt|date("d/m/Y")}} </em></p>
                <p> Etat : {{article.status}}</p>
                <p>Note : {{articleScore}}</p>
            </div>
            
        </div>
    </main>
    
</body>
</html>